import { OrderByConfirmPipe } from './order-by-confirm.pipe';

describe('OrderByConfirmPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByConfirmPipe();
    expect(pipe).toBeTruthy();
  });
});
